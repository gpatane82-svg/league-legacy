# Directory Card v10.1

Implemented the approved final adjustments:

- ARCHIVED uses the exact same 4-degree angle, position, size, opacity, and transform origin as ACTIVE.
- Changed `CAREER RECORD` to `RECORD`.
- Centered and evenly spaced all four stat headers.
- Centered each stat value within its equal-width column while preserving the locked card dimensions.
- No manager portrait assets were changed.
